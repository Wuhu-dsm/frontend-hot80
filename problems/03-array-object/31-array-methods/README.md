# 数组方法实现（map/filter/reduce）

> 分类：数组与对象  
> 环境：node + vitest

## 题目

手写 `myMap` / `myFilter` / `myReduce`，行为对齐原生方法（挂载到原型或独立函数均可，本项目用独立函数）。

## 文件说明

- 实现文件：`index.ts` / `index.tsx` / `index.html`（按题目）
- 测试或验收：`*.test.ts(x)` 或 `checklist.md`

## 开始

```bash
# 跑这一题测试（CSS 题请用 checklist + npm run dev:css）
npm run test -- problems/03-array-object/31-array-methods
```
