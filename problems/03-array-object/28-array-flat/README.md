# 数组扁平化 flat

> 分类：数组与对象  
> 环境：node + vitest

## 题目

实现 `flat(arr, depth = 1)`，支持指定深度，`Infinity` 完全扁平。

## 文件说明

- 实现文件：`index.ts` / `index.tsx` / `index.html`（按题目）
- 测试或验收：`*.test.ts(x)` 或 `checklist.md`

## 开始

```bash
# 跑这一题测试（CSS 题请用 checklist + npm run dev:css）
npm run test -- problems/03-array-object/28-array-flat
```
